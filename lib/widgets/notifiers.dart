import 'package:flutter/material.dart';

// Notifiers for dark mode and child mode
ValueNotifier<bool> darkModeNotifier = ValueNotifier<bool>(false);
ValueNotifier<bool> childModeNotifier = ValueNotifier<bool>(false);
